# FlipGauge165 V3

V3 uses three compact premium module assets: clock, gauge, and date. Dynamic content remains memory-safe: only four main digit bitmaps and two second digit bitmaps are cached at once.

The full-screen background remains vector-rendered to avoid a large 390x390 bitmap allocation.
